_target_: sah.algorithms.gather_activations.ActivationGatherer
network_config:
  _target_: sah.algorithms.gather_activations.NetworkConfig
  _recursive_: false
  _convert_: object
  pretrained_model_name_or_path: HuggingFaceTB/SmolLM2-360M-intermediate-checkpoints
  revision: step-160000
tokenizer_config:
  _target_: sah.algorithms.gather_activations.TokenizerConfig
  _recursive_: false
  _convert_: object
  pretrained_model_name_or_path: HuggingFaceTB/SmolLM2-360M
  use_fast: true
  trust_remote_code: true
dataset_config:
  _target_: sah.algorithms.gather_activations.DatasetConfig
  dataset_path: tatsu-lab/alpaca
  dataset_name: default
  per_device_eval_batch_size: 8
  block_size: 512
  overwrite_cache: false
saving_config:
  _target_: sah.algorithms.gather_activations.SavingConfig
  output_dir: ${hydra:runtime.output_dir}

seed: 42
