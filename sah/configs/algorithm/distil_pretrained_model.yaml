_target_: sah.algorithms.distil_pretrained_model.DistilPretrainedModel
tokenizer_config:
  _target_: sah.algorithms.llm_finetuning.TokenizerConfig
  _recursive_: false
  _convert_: object
  # Use the same key as in the network config. Avoids having to duplicate the value.
  pretrained_model_name_or_path: ${..pretrained_config.pretrained_model_name_or_path}
  use_fast: true
  trust_remote_code: true
pretrained_config:
  _target_: sah.algorithms.llm_finetuning.NetworkConfig
  _recursive_: false
  _convert_: object
  pretrained_model_name_or_path: Qwen/Qwen2.5-1.5B
checkpoint_config:
  _target_: sah.algorithms.distil_pretrained_model.CheckpointConfig
  path: /network/scratch/b/brownet/hydra-runs/finetune-on-lima/checkpoints/finetuned
finetuned_config:
  _target_: sah.algorithms.llm_finetuning.NetworkConfig
  _recursive_: false
  _convert_: object
  pretrained_model_name_or_path: Qwen/Qwen2.5-1.5B
data_dir: "/network/scratch/b/brownet/synthetic-data/finetuned-model-data-qwen"
batch_size: 16
