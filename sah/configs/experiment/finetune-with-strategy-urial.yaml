# @package _global_

defaults:
  - finetune-with-strategy
  - override /algorithm/strategy: urial

trainer:
  enable_checkpointing: false
  max_epochs: 0
  limit_train_batches: 0
  callbacks:
    model_checkpoint: null
    early_stopping: null
    gpu_memory_tracker: null

algorithm:
  experiment_name: urial
