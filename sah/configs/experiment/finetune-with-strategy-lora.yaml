# @package _global_

defaults:
  - finetune-with-strategy
  - override /algorithm/strategy: lora

trainer:
  callbacks:
    model_checkpoint:
      filename: llama-lora-1
  devices: 3

algorithm:
  batch_size: 2
  experiment_name: llama-lora-1
  strategy:
    r: 1
    lr: 1e-4
