# @package _global_

defaults:
  - override /algorithm: estimate_grammar_entropy_bottleneck
  - override /trainer/callbacks: default

trainer:
  enable_checkpointing: false
  limit_val_batches: 0.0
  val_check_interval: 0
  max_epochs: 5
  callbacks:
    model_checkpoint: null

name: entropy-bottleneck
