# @package _global_

defaults:
  - finetune-with-strategy
  - override /algorithm/strategy: lm_head

trainer:
  enable_checkpointing: false
  max_epochs: 4
  callbacks:
    model_checkpoint: null
    early_stopping: null
  val_check_interval: 1.0

algorithm:
  experiment_name: lm_head
