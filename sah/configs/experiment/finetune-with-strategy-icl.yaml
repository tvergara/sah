# @package _global_

defaults:
  - finetune-with-strategy
  - override /algorithm/strategy: icl

trainer:
  # callbacks:
    # model_checkpoint:
    #   filename: llama-lora-1
  devices: 1
  limit_val_batches: 4
  val_check_interval: 1
  

algorithm:
  batch_size: 1
  max_examples: 128
  experiment_name: icl
  max_length: 2048
  val_batch_size: 32

  # strategy:
  #   r: 1
  #   lr: 1e-4
