# @package _global_

defaults:
  - finetune-with-strategy
  - override /algorithm/strategy: adam

trainer:
  callbacks:
    model_checkpoint:
      filename: adam
  devices: 2

algorithm:
  batch_size: 6
  experiment_name: adam
