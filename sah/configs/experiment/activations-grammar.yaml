# @package _global_

defaults:
  - override /algorithm: activations_grammar_model
  - override /trainer/callbacks: default

trainer:
  enable_checkpointing: false
  limit_val_batches: 0.0
  val_check_interval: 0
  max_epochs: 1
  callbacks:
    model_checkpoint: null

name: activations-grammar
