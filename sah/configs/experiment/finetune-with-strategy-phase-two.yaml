# @package _global_

defaults:
  - finetune-with-strategy
  - override /algorithm/strategy: phase_two

trainer:
  enable_checkpointing: false
  callbacks:
    model_checkpoint: null
    early_stopping: null
  val_check_interval: 1.0

algorithm:
  experiment_name: phase-two
